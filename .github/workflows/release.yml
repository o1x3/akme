name: release

on:
  push:
    branches: [main]
    paths:
      - VERSION
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release-version:
    if: ${{ github.ref_type == 'branch' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - uses: actions/setup-node@v7.0.0
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - name: Validate version
        id: version
        run: |
          scripts/validate-version.sh
          version="$(cat VERSION)"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "tag=v$version" >> "$GITHUB_OUTPUT"

      - name: Create release tag
        run: |
          tag="${{ steps.version.outputs.tag }}"

          if git rev-parse -q --verify "refs/tags/$tag" >/dev/null; then
            echo "::error::Tag $tag already exists. Bump VERSION before releasing."
            exit 1
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "$tag" -m "Release $tag"
          git push origin "$tag"

      - uses: goreleaser/goreleaser-action@v7.2.3
        with:
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Publishes npm package `akme` at the same VERSION. The package downloads
      # the GitHub release binary on install / first run, so goreleaser must
      # finish first. Requires repo secret NPM_TOKEN — a granular Automation
      # token that bypasses 2FA. Classic/publish tokens with account 2FA fail
      # with EOTP in CI and cannot publish.
      - name: Publish akme to npm
        working-directory: npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -z "${NODE_AUTH_TOKEN:-}" ]; then
            echo "::error::NPM_TOKEN secret is not set; cannot publish akme@${{ steps.version.outputs.version }}"
            exit 1
          fi

          version="${{ steps.version.outputs.version }}"
          npm version "$version" --no-git-tag-version --allow-same-version

          set +e
          publish_log="$(mktemp)"
          npm publish --access public >"$publish_log" 2>&1
          status=$?
          set -e
          cat "$publish_log"

          if [ "$status" -ne 0 ]; then
            if grep -q "EOTP" "$publish_log"; then
              echo "::error::npm publish failed with EOTP. NPM_TOKEN must be a granular Automation token that bypasses 2FA (https://www.npmjs.com/settings/~/tokens). Replace the repo secret and bump VERSION to release again."
            fi
            exit "$status"
          fi
